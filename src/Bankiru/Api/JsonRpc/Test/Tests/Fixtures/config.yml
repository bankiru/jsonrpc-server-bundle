framework:
    test: ~
    secret:          test
    router:
        resource: "%kernel.root_dir%/routing.yml"
    templating:
        engines: ['twig']

services:
  logger:
    class: Psr\Log\NullLogger

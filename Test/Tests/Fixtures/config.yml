framework:
    test: ~
    secret:       test
    assets:       false
    router:
        resource: "%kernel.root_dir%/routing.yml"
    templating:   false
    translator: ~

services:
  logger:
    class: Psr\Log\NullLogger

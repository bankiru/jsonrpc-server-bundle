framework:
    test: ~
    secret:       test
    assets:       false
    router:
        resource: "%kernel.root_dir%/routing.yml"
    templating:
        engines: ['twig']
    translator: ~

services:
  logger:
    class: Psr\Log\NullLogger
